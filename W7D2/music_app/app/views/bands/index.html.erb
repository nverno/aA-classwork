<%= render 'shared/errors' %>

<h2>Bands</h2>

<ul>
  <% @bands.each do |band| %>
    <li>(<%= band.id %>): <%= link_to band.name, band_url(band) %></li>
  <% end %>
</ul>

<%= link_to "Add Band", new_band_url %>
